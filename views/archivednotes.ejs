<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>All notes</title>
    <link rel="stylesheet" href="/css/notes.css">
</head>
<body>
    <% notes.forEach((note) => { %>
        <p class="note"><span class="username"><%= note.username %></span> <span class="username"> <%= moment(note.date).format('DD MMM YYYY') %> </span> <span> <%= note.note %></span> </p>
    <% }) %>
</body>
</html>