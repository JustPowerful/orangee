<!DOCTYPE html>
<html lang="en">
    <head>
        <%- include("../base.ejs") %>
    </head>
<body>
    <br><br><br>
    <div class="card">
        <img src="/images/default/profile.gif" alt="User pfp" style="width:100%">
        <h1><%= user.username %></h1>  
        <p class="title">
            <% if(user.admin > 0) { %>
                <!-- if user is admin (display something) -->
                <span>Administrator(in)</span>
            <% } else { %>
                <span>Mitarbeiter(in)</span>
            <% } %> 
    </p>
        <% if(user.admin > 0) { %>
            <!-- if user is admin (display something) -->
            <p>
                <a href="/user/settings" class="settingsBtn"><i class="fas fa-cog"></i> die Einstellungen</a>
            </p>
        <% }%>
        
      </div>

    <style>
        .card {
          box-shadow: 0 4px 8px 0 rgba(0, 0, 0, 0.2);
          max-width: 300px;
          margin: auto;
          text-align: center;
          font-family: arial;
        }
        
        .title {
          color: grey;
          font-size: 18px;
        }
        
        .settingsBtn {
          border: none;
          outline: 0;
          display: inline-block;
          padding: 8px;
          color: white;
          background-color: #000;
          text-align: center;
          cursor: pointer;
          width: 100%;
          font-size: 18px;
          
        }
        
        
        .settingsBtn:hover, a:hover {
          opacity: 0.7;
          color: white;
          text-decoration: none;
        }
        </style>
</body>
</html>